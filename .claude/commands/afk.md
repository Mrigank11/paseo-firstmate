---
description: Enter away mode — batch fleet updates into periodic digests instead of per-event pings.
---
The captain is going AFK. Load the `afk-mode` skill and enter AFK per its instructions. If a cadence is given in $ARGUMENTS use it; otherwise default to every 30 minutes.
